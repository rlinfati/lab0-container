version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: docker
    directories:
      - /grbts      # ubi-micro ubi-minimal
      - /rbackup    # ubi
      - /strongswan # fedora
    schedule:
      interval: weekly
